{% extends 'layout.html.twig' %}

{% block title %}Confitures{% endblock %}

{% block body %}
<div id="card">
{% for recette in recettes %}

    <a href="{{ path('app_recette_view', {titre: recette.titre}) }}" class="card-link">
        <div class="card m-3" style="width: 15rem; ">
            <img src="{{ recette.image }}" class="card-img-top" alt="" style="width: 100%">
            <div class="card-body">
            <h5 class="card-title fw-bolder">{{ recette.titre}}</h5>
            <p class="card-text">{{ recette.instruction | u.truncate(100, '...')  | raw}}</p>
            </div>
    </a>
            <ul class="list-group list-group-flush">
            <li class="list-group-item">{{recette.DatePublication | date}}</li>
            </ul>
            <div class="card-body"></div>
        </div>

    {% endfor %}
</div>

{% endblock %}
